#!/bin/sh
# Use commitment to generate its own commit messages (dogfooding!)
# Only run for regular commits (not merge, squash, etc.)
if [ -z "$2" ]; then
  ./dist/cli.js --message-only > "$1" || exit 1
fi
