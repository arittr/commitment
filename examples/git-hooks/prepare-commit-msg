#!/bin/sh
# Git hook for commitment (without Husky)
# Place this in .git/hooks/prepare-commit-msg

# Only run for regular commits
if [ -z "$2" ]; then
  echo "🤖 Generating commit message..." > /dev/tty 2>/dev/null || true
  npx @arittr/commitment --message-only > "$1" || true
fi
